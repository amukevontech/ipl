'use strict';

module.exports = function(Fielderscores) {
    var app = require('../../server/server');

    // Fielderscores.observe('after save', function(ctx, next){
    //     app.io.emit('listPlayerPoints', {data: 'new record is inserted'});
    // });

};
